---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/x-icon" href="/gskis.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>GSkis</title>
		<style>
			@font-face {
				font-family: 'VCR';
				src: url('/fonts/VCR.ttf') format('truetype');
				font-display: swap;
			}
			html, body {
				height: 100%;
				margin: 0;
				padding: 0;
				box-sizing: border-box;
				-webkit-user-select: none;
				-moz-user-select: none;
				-ms-user-select: none;
				user-select: none;
			}
			body {
				min-height: 100vh;
				min-width: 100vw;
				overflow: hidden;
				position: relative;
				background: #0a0f0a;
				font-family: system-ui, sans-serif;
			}
			.terminal-bg {
				position: fixed;
				top: 0; left: 0; right: 0; bottom: 0;
				width: 100vw; height: 100vh;
				z-index: 1;
				pointer-events: none;
				overflow: hidden;
			}
			.terminal-line {
				font-family: 'VCR', monospace;
				color: #39ff14;
				font-size: clamp(1.2rem, 4vw, 2.8rem);
				opacity: 0.7;
				white-space: nowrap;
				will-change: transform;
				text-shadow: 0 0 8px #39ff14, 0 0 2px #39ff14;
				position: absolute;
				right: -100vw;
				left: unset;
				animation: slide-left 30s linear infinite;
			}
			.terminal-line:nth-child(2) { top: 20vh; animation-delay: 2s; }
			.terminal-line:nth-child(3) { top: 30vh; animation-delay: 4s; }
			.terminal-line:nth-child(4) { top: 45vh; animation-delay: 6s; }
			.terminal-line:nth-child(5) { top: 60vh; animation-delay: 8s; }
			.terminal-line:nth-child(6) { top: 75vh; animation-delay: 10s; }
			@keyframes slide-left {
				0% { transform: translateX(0); }
				100% { transform: translateX(-300vw); }
			}
			.overlay {
				position: fixed;
				top: 0; left: 0; right: 0; bottom: 0;
				width: 100vw; height: 100vh;
				background: rgba(10, 15, 10, 0.82);
				z-index: 2;
				pointer-events: none;
			}
			.content {
				position: relative;
				z-index: 3;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				min-height: 100vh;
				text-align: center;
			}
			.brand {
				font-family: 'VCR', monospace;
				color: #39ff14;
				font-size: clamp(2.5rem, 10vw, 5.5rem);
				letter-spacing: 0.08em;
				margin-bottom: 1.5rem;
				text-shadow: 0 0 12px #39ff14, 0 0 2px #39ff14;
				text-align: center;
				padding: 0 1rem;
			}
			.contact-btn {
				background: #39ff14;
				color: #0a0f0a;
				font-family: 'VCR', monospace;
				font-size: clamp(1.2rem, 4vw, 2.5rem);
				border: none;
				border-radius: 0.4em;
				padding: 0.8em 2.2em;
				cursor: pointer;
				box-shadow: 0 0 8px #39ff14, 0 0 2px #39ff14;
				transition: background 0.2s, color 0.2s, transform 0.1s;
				margin-top: 1.5rem;
				text-decoration: none;
				min-width: 200px;
				text-align: center;
			}
			.contact-btn:hover, .contact-btn:focus {
				background: #0a0f0a;
				color: #39ff14;
				outline: 2px solid #39ff14;
				transform: scale(1.04);
			}
			.footer {
				position: fixed;
				bottom: 0;
				left: 0;
				right: 0;
				text-align: center;
				padding: 0.8rem;
				font-family: 'VCR', monospace;
				color: #39ff14;
				font-size: clamp(0.9rem, 3vw, 1.5rem);
				z-index: 3;
				text-shadow: 0 0 4px #39ff14;
			}
			@media (max-width: 768px) {
				.brand { 
					font-size: 3.5rem; 
					margin-bottom: 1rem;
					padding: 0 0.5rem;
				}
				.contact-btn { 
					font-size: 1.4rem; 
					padding: 0.7em 1.5em; 
					min-width: 180px;
					margin-top: 1rem;
				}
				.terminal-line { 
					font-size: 1.1rem; 
					opacity: 0.6;
					max-width: 85vw;
				}
				.footer { 
					font-size: 0.9rem; 
					padding: 0.6rem;
				}
				.content {
					padding: 0 1rem;
				}
			}
			@media (max-width: 480px) {
				.brand { 
					font-size: 2.8rem; 
				}
				.contact-btn { 
					font-size: 1.2rem; 
					padding: 0.6em 1.2em;
					min-width: 160px;
				}
				.terminal-line { 
					font-size: 1rem;
					opacity: 0.5;
				}
				.footer { 
					font-size: 0.8rem; 
				}
			}
		</style>
	</head>
	<body>
		<div class="terminal-bg">
			<div class="terminal-line" style="top: 10vh;">We started as a small group, trying to solve problems close to home.</div>
			<div class="terminal-line">Each challenge lit the spark for something new.</div>
			<div class="terminal-line">We build for ourselves first, and in doing so, for others too.</div>
			<div class="terminal-line">We're our own toughest critics—and our most honest users.</div>
			<div class="terminal-line">But every decision is made with others in mind.</div>
			<div class="terminal-line">What began as personal need became something bigger. This is GSkis.</div>
		</div>
		<div class="overlay"></div>
		<main class="content">
			<div class="brand">GSkis</div>
			<a href="mailto:hello@gskis.com" class="contact-btn">Contact Us</a>
		</main>
		<footer class="footer">
			GSkis LLC 2025 • Boston MA
		</footer>
	</body>
</html>
